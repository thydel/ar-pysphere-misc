---

- vsphere_extraconfig:
    vcenter_hostname: vcenter.mydomain.local
    username: myuser
    password: mypass
    guest: newvm001
    xtracnfset:
      disk.locking : "false"
      disk.EnableUUID: "true"
